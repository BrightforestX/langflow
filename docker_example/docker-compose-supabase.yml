version: "3.8"

services:
  langflow:
    image: langflowai/langflow:latest
    ports:
      - "7860:7860"
    depends_on:
      - postgres
    environment:
      - LANGFLOW_DATABASE_URL=postgresql://postgres.kwmbphdnzbmhiavfjhjf:4TMzluzrK58EvvGT@aws-0-us-east-1.pooler.supabase.com:6543/postgres
      # This variable defines where the logs, file storage, monitor data and secret keys are stored.
    volumes:
      - langflow-data:/app/langflow

  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres.kwmbphdnzbmhiavfjhjf
      POSTGRES_PASSWORD: 4TMzluzrK58EvvGT
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - langflow-postgres:/var/lib/postgresql/data

volumes:
  langflow-postgres:
  langflow-data:
